<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>header</title>
  </head>
  <body>
    <nav class="bg-white shadow-md sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <!-- Left side - Logo -->
          <div class="flex items-center">
            <a href="/" class="flex items-center">
              <span
                class="text-2xl font-bold text-rose-500 hover:text-rose-600 transition-colors"
              >
                Airbnb
              </span>
            </a>
          </div>

          <!-- Center - Navigation Links -->
          <div class="hidden md:flex items-center space-x-4">
            <% if(isLoggedIn) { %>
            <a
              href="/homeList"
              class="px-3 py-2 rounded-md text-sm font-medium <%= currentPage === 'home' ? 'text-rose-500 bg-rose-50' : 'text-gray-700 hover:text-rose-500 hover:bg-rose-50' %> transition-all"
            >
              Home
            </a>
            <a
              href="/favourites"
              class="px-3 py-2 rounded-md text-sm font-medium <%= currentPage === 'favourites' ? 'text-rose-500 bg-rose-50' : 'text-gray-700 hover:text-rose-500 hover:bg-rose-50' %> transition-all"
            >
              Favourites
            </a>
            <a
              href="/bookings"
              class="px-3 py-2 rounded-md text-sm font-medium <%= currentPage === 'bookings' ? 'text-rose-500 bg-rose-50' : 'text-gray-700 hover:text-rose-500 hover:bg-rose-50' %> transition-all"
            >
              Bookings
            </a>
            <a
              href="/host/host-home-list"
              class="px-3 py-2 rounded-md text-sm font-medium <%= currentPage === 'hostHomes' ? 'text-rose-500 bg-rose-50' : 'text-gray-700 hover:text-rose-500 hover:bg-rose-50' %> transition-all"
            >
              Host Homes
            </a>
            <a
              href="/host/add-home"
              class="px-3 py-2 rounded-md text-sm font-medium <%= currentPage === 'addHome' ? 'text-rose-500 bg-rose-50' : 'text-gray-700 hover:text-rose-500 hover:bg-rose-50' %> transition-all"
            >
              Add Home
            </a>
            <% } %>
          </div>

          <!-- Right side - Auth Buttons -->
          <div class="flex items-center space-x-2">
            <% if(!isLoggedIn) { %>
            <a
              href="/signup"
              class="px-4 py-2 text-sm font-medium <%= currentPage === 'signup' ? 'text-white bg-rose-500' : 'text-gray-700 hover:text-rose-500' %> rounded-full transition-all"
            >
              Sign up
            </a>
            <a
              href="/login"
              class="px-4 py-2 text-sm font-medium <%= currentPage === 'login' ? 'text-white bg-rose-500' : 'text-gray-700 hover:text-rose-500' %> rounded-full transition-all"
            >
              Login
            </a>
            <% } else { %>
            <form action="/logout" method="POST">
              <button
                type="submit"
                class="px-4 py-2 text-sm font-medium text-gray-700 hover:text-rose-500 rounded-full transition-all"
              >
                Logout
              </button>
            </form>
            <% } %>
          </div>
        </div>
      </div>
    </nav>
  </body>
</html>
