<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>header</title>
  </head>
  <body>
    <nav class="bg-white shadow">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex gap-5 h-16">
          <div class="flex">
            <a
              href="/"
              class="inline-flex items-center px-4 py-2 text-base font-medium text-gray-900 <%= currentPage === 'index' ? 'text-rose-500' : 'hover:text-rose-500'%>"
            >
              Airbnb
            </a>
          </div>
          <% if(isLoggedIn) { %>
          <div class="flex">
            <a
              href="/homeList"
              class="inline-flex items-center px-4 py-2 text-base font-medium text-gray-900 <%= currentPage === 'home' ? 'text-rose-500' : 'hover:text-rose-500'%>"
            >
              Home
            </a>
          </div>
          <div class="flex">
            <a
              href="/favourites"
              class="inline-flex items-center px-4 py-2 text-base font-medium text-gray-900 <%= currentPage === 'favourites' ? 'text-rose-500' : 'hover:text-rose-500'%>"
            >
              Favourites
            </a>
          </div>
          <div class="flex items-center">
            <a
              href="/bookings"
              class="inline-flex items-center px-4 py-2 text-base font-medium <%= currentPage === 'bookings' ? 'text-rose-500' : 'hover:text-rose-500'%>"
            >
              Bookings
            </a>
          </div>
          <div class="flex items-center">
            <a
              href="/host/host-home-list"
              class="inline-flex items-center px-4 py-2 text-base font-medium <%= currentPage === 'hostHomes' ? 'text-rose-500' : 'hover:text-rose-500'%>"
            >
              Host Homes
            </a>
          </div>
          <div class="flex items-center">
            <a
              href="/host/add-home"
              class="inline-flex items-center px-4 py-2 text-base font-medium <%= currentPage === 'addHome' ? 'text-rose-500' : 'hover:text-rose-500'%>"
            >
              Add Home
            </a>
          </div>
          <% } %>
          <div class="flex items-center">
            <% if(!isLoggedIn) { %>
            <a
              href="/login"
              class="inline-flex items-center px-4 py-2 text-base font-medium <%= currentPage === 'login' ? 'text-rose-500' : 'hover:text-rose-500'%>"
            >
              Login
            </a>
            <% } else { %>
            <form action="/logout" method="POST">
              <button
                type="Sumbit"
                class="inline-flex items-center px-4 py-2 text-base font-medium <%= currentPage === 'login' ? 'text-rose-500' : 'hover:text-rose-500'%>"
              >
                Logout
              </button>
            </form>
            <% } %>
          </div>
        </div>
      </div>
    </nav>
  </body>
</html>
